// ============================================================
// FINANCEOS - SCHEDULER SERVICE (BACKWARDS COMPATIBILITY WRAPPER)
// Delegates directly to centralized services/schedulerService.js
// ============================================================

const schedulerService = require("../services/schedulerService");

module.exports = schedulerService;
