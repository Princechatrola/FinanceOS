// ============================================================
// FINANCEOS - EMAIL SERVICE (BACKWARDS COMPATIBILITY WRAPPER)
// Delegates directly to centralized services/emailService.js
// ============================================================

const emailService = require("../services/emailService");

module.exports = emailService;
